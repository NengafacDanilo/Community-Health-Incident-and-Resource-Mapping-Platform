
  # Create Gradient Background

  This is a code bundle for Create Gradient Background. The original project is available at https://www.figma.com/design/6Hh05S8x7WoTDMF01MZQ2q/Create-Gradient-Background.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  